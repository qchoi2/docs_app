# 색인 업데이트 설명서

이 문서는 새로운 M&A 계약서를 기존 색인과 운영 DB에 추가할 때 필요한
파일, 전달 방식, 요청 문구와 검증 절차를 정리한 운영 지침이다.

현재 기준은 V4 taxonomy version 8이며, 진술·보장(RW), 선행조건(CP),
확약(COV), 정의(DEF), 대금·지급구조(PAY), 손해배상·구제(REM)를 원자
명제 단위로 색인한다.

---

## 1. 기본 원칙

색인 업데이트는 다음 원칙에 따라 수행한다.

1. 새 계약서는 기존 계약서와 동일한 분류·원자화 기준으로 처리한다.
2. 동일한 법적 명제는 표현이 달라도 기존의 동일한 taxonomy ID로 분류한다.
3. 기존 taxonomy로 설명할 수 없는 새로운 명제만 신규 후보로 만든다.
4. 신규 후보는 기존 노드와의 중복, 단순 표현 차이, 상·하위 관계와 오추출 여부를
   문맥으로 확인한 후 다음 중 하나로 처리한다.
   - 기존 노드 사용
   - 기존 노드의 alias로 병합
   - 신규 leaf 노드 생성
   - 오추출·무의미 후보로 반려
5. 본문이 별지, Disclosure Schedule, Schedule, Annex, Exhibit 등을 참조하면
   해당 자료도 함께 확인한다.
6. 본문 또는 별지가 일부만 확인된 문서는 `partial` 또는 `not_evaluated`로
   저장한다. 키워드 미검출이나 부분 검토만으로 조항의 부재를 판정하지 않는다.
7. 원문, file_key, ¶ 위치와 taxonomy 판단 근거가 확인된 item만 운영 DB에 적재한다.
8. taxonomy 변경과 DB 적재 후에는 감사와 검색 회귀 테스트를 수행한다.

---

## 2. 전달해야 할 파일

### 2.1 가장 권장하는 전달 방식

다음 두 폴더를 원래 구조 그대로 전달하는 것이 가장 안전하다.

```text
index_update_package/
├─ new_contracts/
│  ├─ 거래A/
│  │  ├─ 주식매매계약서.docx
│  │  ├─ 별지 1.pdf
│  │  └─ 매도인 공개사항.xlsx 또는 별도 문서
│  └─ 거래B/
│     ├─ Asset Purchase Agreement.pdf
│     └─ Seller Disclosure Schedule.pdf
└─ cs_index/
   ├─ catalog.sqlite
   ├─ txt/
   └─ 기존 색인 산출물
```

필수 파일은 다음과 같다.

#### A. 새 계약서 원본

- `.docx`, `.doc`, `.pdf` 등 계약서 원본
- 계약서가 참조하는 별지·별첨·부속서
- Seller/Buyer Disclosure Schedule
- Amendment, Restatement, Side Letter 등 본문 내용을 변경하는 관련 계약
- 동일 거래의 국문·영문본이 모두 있으면 양쪽 모두

파일명과 거래별 폴더 구조를 유지한다. 별지 파일을 본문과 다른 폴더에
분리하면 연결이 어려워질 수 있으므로 같은 거래 폴더에 둔다.

#### B. 기존 운영 색인

최소한 다음 파일이 필요하다.

```text
cs_index/catalog.sqlite
cs_index/txt/
```

`catalog.sqlite`에는 파일 메타데이터, 기존 doc_meta, V4 taxonomy, aliases,
원자 item과 coverage 상태가 저장된다. `txt/`에는 원문 근거의 ¶ 좌표가 있는
추출 텍스트가 저장된다.

가능하면 `catalog.sqlite`만 따로 보내지 말고 **전체 `cs_index` 폴더**를
전달한다. 전체 폴더에는 이전 감사 결과, manifest, 추출 결과와 버전 이력이
포함되어 있어 업데이트 검증과 문제 복구가 쉬워진다.

### 2.2 새 세션·새 컴퓨터에서 추가로 필요한 파일

현재 프로젝트 워크스페이스가 유지되지 않는 환경에서는 다음 기준 파일도
함께 전달한다.

```text
AGENTS.md
v4_schema.py
plan_v4_batch.py
audit_t3_v4.py
store_v4_results.py
index_contracts.py
data/term_dict.yaml
data/v4_term_mapping.yaml
.docs/extract_prompt_v4.md
색인 업데이트 설명서.md
```

가장 간단한 방법은 `contract_docs` 등 기존 대용량 원본 폴더를 제외한
프로젝트 폴더 전체와 `cs_index`를 함께 압축하는 것이다.

### 2.3 있으면 도움이 되는 자료

- 새 계약서 목록과 예상 건수
- 거래별 계약 유형: SPA, SSA, SHA, ATA/BTA 등
- 초안·체결본 여부
- 국문·영문 여부
- 동일 거래의 여러 버전 중 최신본 표시
- 기존 파일을 수정·대체한 것인지, 완전히 신규인지를 설명한 목록
- 특히 중요하게 검색하려는 조항이나 거래구조
- OCR이 필요한 스캔 PDF라는 표시

아래와 같은 `업데이트 요청 메모.md`를 새 계약서와 함께 넣어도 된다.

```markdown
# 업데이트 요청 메모

- 신규 계약서 예상 수: 12건
- 기존 계약서 수정본: 2건
- 계약 유형: SPA 7건, SHA 3건, ATA/BTA 4건
- 언어: 국문 9건, 영문 5건
- 운영 DB 반영: 승인
- 신규 taxonomy 처리: 기존 노드 중복 검토 후 필요한 경우 신규 leaf 생성 승인
- 별지 포함 여부: 전부 포함
- 특별 확인사항: 언아웃, RWI, 제3자청구 절차
```

---

## 3. 전달 전에 확인할 사항

### 3.1 프로그램과 DB 종료

`catalog.sqlite`를 사용하는 웹앱이나 검색 프로그램을 종료한 뒤 복사한다.
SQLite가 실행 중이면 최신 변경이 `catalog.sqlite-wal`에 남아 있을 수 있다.

- 가장 안전한 방법: 프로그램 종료 후 `catalog.sqlite` 복사
- 프로그램을 종료할 수 없다면 `catalog.sqlite`, `catalog.sqlite-wal`,
  `catalog.sqlite-shm`을 함께 전달

### 3.2 계약서와 별지 확인

- 본문에서 언급한 별지 파일이 실제로 포함되어 있는지 확인한다.
- 암호가 설정된 PDF는 암호를 별도의 안전한 방법으로 알려준다.
- 이미지 스캔 PDF는 OCR 필요 여부를 표시한다.
- 일부 별지를 의도적으로 제외한 경우 그 사실을 명시한다.

### 3.3 보안정보 제외

다음 자료는 색인 업데이트 패키지에 넣지 않는다.

- API 키
- 비밀번호
- 개인 인증서
- 브라우저 세션 파일
- 운영 서버 접속정보

계약서 내용 자체를 가명처리하거나 일부 삭제했다면, 검색결과의 한계가 생길 수
있으므로 삭제·가명처리 범위를 함께 설명한다.

---

## 4. 요청 문구

### 4.1 권장 요청 문구

다음 문구를 그대로 사용할 수 있다.

> 첨부한 `new_contracts`와 기존 `cs_index`를 사용해 색인 업데이트를 진행해줘.
> 신규·변경 파일만 증분 처리하고, 현재 V4 taxonomy와 원자화 기준을 적용해줘.
> 본문이 참조하는 별지와 Disclosure Schedule도 확인해줘. 기존 명제와 같은
> 내용은 같은 taxonomy ID로 분류하고, 기존 taxonomy로 설명할 수 없는 명제는
> 중복 여부를 문맥으로 검토한 뒤 필요한 경우 신규 leaf를 추가해줘. 감사에
> 통과한 확정 item은 운영 DB에 적재해줘. 부분 검토 문서는 `partial`,
> 미확인 별지는 `not_evaluated`로 유지하고 부재 판정에 사용하지 마.
> 완료 후 신규·변경 문서 수, taxonomy 변경, DB 적재 수, 미해결 후보,
> 감사·회귀검사 결과와 산출물 링크를 보고해줘.

### 4.2 신규 taxonomy 판단까지 위임하는 요청

이번 구축 과정과 같이 taxonomy 판단까지 맡기려면 다음 문장을 추가한다.

> 신규 taxonomy 후보에 대해서는 기존 노드·alias와 의미가 같은지, 기존 노드의
> 단순 하위 구성요소인지, 독립 검색가치가 있는 법적 명제인지를 문맥으로
> 판단해줘. 독립적인 신규 명제이고 근거가 명확한 경우 신규 leaf 생성과
> taxonomy version 증가를 승인한다. 애매한 후보는 자동 승격하지 말고
> `needs_review`로 남겨줘.

### 4.3 taxonomy를 먼저 검토하고 DB 적재를 나중에 하는 요청

더 보수적으로 운영하려면 다음과 같이 요청한다.

> 1단계에서는 신규 계약서 분석, 원자 item 초안과 taxonomy 후보만 만들어줘.
> 운영 DB에는 아직 적재하지 말고 후보와 근거를 먼저 보여줘. 내가 승인한 후
> 2단계에서 taxonomy와 운영 DB에 반영해줘.

### 4.4 기존 taxonomy만 사용하는 요청

신규 노드를 만들지 않으려면 다음과 같이 요청한다.

> 신규 계약서를 기존 taxonomy로만 분류해줘. 대응 노드가 없는 명제는 신규
> 후보로 기록하되 taxonomy와 운영 DB의 정식 노드에는 추가하지 말아줘.

---

## 5. 업데이트 작업 절차

업데이트 요청을 받으면 다음 순서로 작업한다.

### 1단계 — 패키지 무결성 확인

- `catalog.sqlite`와 `txt/` 존재 확인
- 현재 taxonomy version과 item 수 확인
- DB 손상 여부와 쓰기 가능 여부 확인
- 계약서 원본 및 별지 목록 확인
- 기존 file_key·content_hash와 비교해 신규·변경·동일 파일 구분

이 단계에서 원본 계약서나 DB를 수정하지 않는다.

### 2단계 — 백업

DB를 변경하기 전에 기존 `catalog.sqlite`의 백업을 만든다. 백업파일에는
업데이트 일시 또는 batch label을 붙인다.

예:

```text
catalog_before_update_20260723.sqlite
```

### 3단계 — 증분 기본 색인

- 신규 파일만 추출·색인
- 동일 내용의 중복 파일은 dup_group으로 연결
- 내용이 변경된 기존 파일은 새 content_hash로 처리
- 삭제·이동 파일은 기존 정책에 따라 `missing` 또는 이동으로 기록
- 원문 텍스트에 `[¶n]` 좌표 생성

### 4단계 — 계약 구조 및 별지 인벤토리 작성

- 계약 유형·언어·초안 여부 확인
- RW, CP, COV, DEF, PAY, REM 관련 범위 확인
- 본문에서 참조하는 별지·Disclosure Schedule 탐지
- 별지 파일의 존재·판독 가능성·저장 file_key 연결

### 5단계 — V4 원자화

각 문서에서 독립적인 법적 명제를 분리한다.

- 하나의 문단에 여러 의무·진술·조건이 있으면 여러 item으로 분리
- 가능한 한 가장 구체적인 기존 leaf taxonomy 사용
- 주체, 상대방, 행위, 대상, 시점, 금액·기간·비율과 qualifier 기록
- 원문 verbatim과 ¶ 위치 기록
- 정의, 대금구조, 손해배상 trigger와 청구절차도 독립 item으로 분리
- 본문과 별지 item을 source_kind와 parent_clause_ref로 연결

### 6단계 — taxonomy 후보 판정

기존 taxonomy와 대응되지 않는 명제를 발견하면 다음 기준으로 판단한다.

1. 같은 의미의 기존 node 또는 alias가 있는가?
2. 단지 주체·금액·기간만 다른 동일 명제인가?
3. 상위 domain의 새로운 독립 구성요소인가?
4. 다른 계약에서도 반복될 가능성과 검색가치가 있는가?
5. 정의·목차·주석·협상 메모를 실제 법적 명제로 오인한 것은 아닌가?

신규 노드는 가능한 한 leaf로 만들고, 상위노드 하나로 여러 명제를 뭉치지 않는다.

### 7단계 — 감사

운영 DB 적재 전에 다음을 검사한다.

- taxonomy ID와 family 일치
- verbatim이 해당 ¶ 범위에 실제 존재하는지
- 원문에 없는 숫자·기간·주체가 보충되지 않았는지
- 독립 명제가 하나의 item에 합쳐지지 않았는지
- 열거된 하위 항·호가 누락되지 않았는지
- 별지 coverage가 실제 상태와 일치하는지
- 신규 후보가 기존 alias와 중복되지 않는지
- `complete`가 실제 전체 검토를 의미하는지

오류, 미확인 후보 또는 low confidence가 있으면 자동 적재하지 않고
`review` 또는 `needs_review`로 분리한다.

### 8단계 — 운영 DB 적재

명시적으로 운영 DB 반영이 승인된 경우에만 적재한다.

- 확정 item → `v4_clause_item`
- 본문·별지 평가상태 → `v4_document_coverage`
- 참조자료별 상태 → `v4_source_coverage`
- 미해결 신규 후보 → `v4_taxonomy_candidate`
- 신규 노드·alias → taxonomy version 증가 후 반영

기존 `doc_meta`와 관련 없는 사용자 데이터를 삭제하거나 초기화하지 않는다.

### 9단계 — 회귀검사와 결과 보고

- 전체 테스트
- term dictionary 및 V4 mapping 검증
- 기존 검색 골든 세트 회귀검사
- DB 누적 item·문서·taxonomy·alias 수 확인
- 신규 item이 `partial` 문서를 부재 판정에 사용하지 않는지 확인

---

## 6. 업데이트 완료 후 받아야 할 결과

완료 보고에는 최소한 다음 내용이 포함되어야 한다.

### 처리 범위

- 전달받은 파일 수
- 신규 계약서 수
- 변경 계약서 수
- 중복·동일 파일 수
- 본문 검색 불가·OCR 필요·오류 파일 수

### Taxonomy 변경

- 업데이트 전후 taxonomy version
- 업데이트 전후 node 및 alias 수
- 새 노드 목록과 parent
- 기존 노드로 병합된 후보
- 반려 또는 미해결 후보와 사유

### 운영 DB 적재

- 적재 문서 수
- 적재 item 수
- family별 item 수
- `approved`, `needs_review`, `rejected` 수
- 누적 문서·item 수
- `complete`, `partial`, `not_evaluated` coverage 수

### 검증

- 감사 pass/review/error 수
- 테스트 결과
- 검색 회귀 결과
- term dictionary·mapping 검증 결과

### 산출물

다음 파일의 링크를 받는 것을 권장한다.

```text
업데이트 최종 보고서.md
taxonomy 변경내역.json
신규 item manifest.json
적재 전 audit.json
적재 결과 audit.json
미해결 taxonomy 후보.json
업데이트 후 catalog.sqlite
업데이트 전 catalog.sqlite 백업
```

---

## 7. 전달 패키지의 권장 이름

업데이트 이력을 구분할 수 있도록 다음 형식을 권장한다.

```text
ma_index_update_YYYYMMDD_batchNN.zip
```

예:

```text
ma_index_update_20260815_batch03.zip
```

압축파일 내부에는 다음과 같이 batch 정보를 남긴다.

```text
ma_index_update_20260815_batch03/
├─ 업데이트 요청 메모.md
├─ new_contracts/
├─ cs_index/
└─ project_rules/       # 새 환경에 전달하는 경우
```

---

## 8. DB만 전달할 때의 주의사항

계약서 원본과 `catalog.sqlite`만 전달해도 업데이트는 가능하지만 다음 작업이
추가될 수 있다.

- 기존 txt 캐시 재생성
- content_hash와 ¶ 좌표 재확인
- 별지 연결정보 복구
- 이전 taxonomy 감사 산출물 재구성
- 원문 갱신 여부 재판정

따라서 DB만 보내는 방식은 긴급 상황에 한해 사용하고, 가능하면 전체
`cs_index` 폴더를 전달한다.

CSV·Excel로 export한 item 목록만으로는 원문 근거, coverage, taxonomy 관계와
별지 연결을 완전히 복원할 수 없으므로 운영 DB 대체물로 사용할 수 없다.

---

## 9. 같은 워크스페이스에서 업데이트하는 경우

이 프로젝트 폴더와 기존 `cs_index`가 그대로 유지되는 경우에는 다음만 추가로
제공하면 된다.

1. 새 계약서와 별지 파일
2. 수정·대체 파일 여부를 설명한 짧은 메모
3. 운영 DB 적재 여부
4. 신규 taxonomy 판단의 위임 범위

이 경우 기준 파일과 기존 DB를 다시 전달할 필요가 없다. 다음과 같이 요청하면 된다.

> 새로 추가한 계약서 폴더를 현재 색인과 비교해서 증분 업데이트해줘.
> 이 설명서와 현재 V4 기준을 적용하고, 확정 item은 운영 DB에 반영해줘.

---

## 10. 새 세션에서의 최소 요청

새 세션에서는 이 문서를 먼저 읽도록 명시한다.

> 먼저 `AGENTS.md`와 `색인 업데이트 설명서.md`를 전부 읽어줘. 첨부한
> `new_contracts`와 `cs_index`를 사용해 현재 V4 기준으로 증분 업데이트해줘.
> 신규 명제는 기존 taxonomy와 의미 중복을 문맥으로 검토하고, 필요한 경우에만
> 신규 leaf를 생성해줘. 확정 item의 운영 DB 적재까지 진행하고 감사·회귀검사
> 결과와 변경내역을 보고해줘.

---

## 11. 현재 기준 파일

업데이트 기준은 다음 파일에 저장되어 있다.

```text
AGENTS.md
.docs/extract_prompt_v4.md
.docs/V4_PLAN.md
v4_schema.py
audit_t3_v4.py
store_v4_results.py
data/v4_term_mapping.yaml
data/term_dict.yaml
```

새로운 업데이트 기준이 승인되면 taxonomy version, prompt version과 변경 보고서를
함께 갱신해야 한다. 과거 item은 해당 item에 저장된 taxonomy·extractor·prompt
version으로 추적할 수 있어야 한다.

---

## 12. 빠른 체크리스트

전달 전:

- [ ] 새 계약서 원본이 포함되어 있다.
- [ ] 본문이 참조하는 별지·Disclosure Schedule이 포함되어 있다.
- [ ] 기존 `cs_index` 전체 또는 최소 `catalog.sqlite`와 `txt/`가 포함되어 있다.
- [ ] SQLite를 사용하는 프로그램을 종료하고 DB를 복사했다.
- [ ] 신규·수정·대체 파일을 구분하는 메모가 있다.
- [ ] 운영 DB 적재 여부를 명시했다.
- [ ] 신규 taxonomy 판단 위임 범위를 명시했다.
- [ ] API 키·비밀번호 등 불필요한 보안정보를 제거했다.

완료 후:

- [ ] 신규·변경·중복·오류 파일 수를 확인했다.
- [ ] taxonomy와 alias 변경내역을 받았다.
- [ ] 운영 DB 적재 item·문서 수를 확인했다.
- [ ] 미해결 후보와 제외 사유를 확인했다.
- [ ] 감사와 검색 회귀 결과를 확인했다.
- [ ] 업데이트 전 DB 백업과 업데이트 후 DB를 보관했다.
