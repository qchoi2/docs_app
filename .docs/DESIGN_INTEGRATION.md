# DESIGN_INTEGRATION — getdesign.md 기반 디자인 자산 활용 지침
_2026-07-08 · UI 구현 전에 반드시 확인할 디자인 연동 절차._

## 1. 목적

웹 UI를 구현할 때 임의의 새 디자인 시스템을 만들지 않고, 사용자가 제공하는 `getdesign.md`와 그 문서가 참조하는 디자인 파일을 우선 활용한다.

## 2. 구현 전 필수 절차

UI 구현 Step에 들어가기 전에 코딩 모델은 다음을 수행한다.

```text
1. 저장소 루트 또는 사용자 지정 위치의 getdesign.md를 찾는다.
2. getdesign.md가 참조하는 디자인 파일, 이미지, 토큰, CSS, 컴포넌트, 스크린샷을 확인한다.
3. 사용할 수 있는 자산을 DESIGN_AUDIT.md에 요약한다.
4. UI_PRODUCT_SPEC.md의 화면 요구사항과 디자인 자산 사이의 충돌을 정리한다.
5. STACK_DECISION.md를 작성해 프론트엔드 스택을 명시한다.
6. 충돌이 없으면 getdesign.md의 색상·폰트·간격·컴포넌트 스타일을 우선 적용한다.
```

## 3. 금지사항

- getdesign.md를 확인하지 않고 UI 스타일을 임의로 만들지 않는다.
- 사용 가능한 디자인 토큰이 있는데 Tailwind/Bootstrap 등 새 디자인 체계를 임의 도입하지 않는다.
- 기능 구현보다 장식적 애니메이션을 우선하지 않는다.
- 디자인 파일이 없다는 이유로 UI 구현 전체를 중단하지 않는다. 이 경우 기능 우선 MVP를 만들고 스타일은 최소화한다.

## 4. DESIGN_AUDIT.md에 포함할 내용

```text
# DESIGN_AUDIT

## 확인한 디자인 소스
- getdesign.md 경로:
- 참조 파일:

## 사용 가능한 토큰
- 색상:
- 폰트:
- 간격:
- radius/shadow:

## 사용 가능한 컴포넌트
- 버튼:
- 입력창:
- 카드:
- 테이블:
- 배지:
- 사이드바:

## UI_PRODUCT_SPEC 적용 계획
- 검색 화면:
- 결과 카드:
- 문단 뷰어:
- 히스토리 사이드바:
- 대시보드:

## 충돌/부족한 부분
- ...

# STACK_DECISION

## 후보
- 서버 렌더링 + 경량 JS(htmx/vanilla)
- SPA(React+Vite 등)
- 기타

## 선택
- 선택한 스택:
- 선택 이유:
- getdesign.md 자산과의 호환성:
- 빌드/배포 복잡도:
- 개인 Windows PC 로컬 단일 사용자 앱 관점의 유지보수성:

## 명시적 배제
- 사용하지 않는 스택:
- 배제 이유:
```

## 5. 우선순위

1. 기능적 명확성
2. 근거 확인 가능성
3. getdesign.md 준수
4. 반응형/시각적 polish

계약서 검색앱이므로 화려한 UI보다 결과 신뢰와 문단 확인이 중요하다.

## 6. UI 구현 명령 예시

```text
UI 구현 전에 getdesign.md를 읽고 DESIGN_AUDIT.md를 작성하라.
getdesign.md가 제공하는 색상, 폰트, 간격, 카드/버튼 스타일을 우선 사용하라.
디자인 자산이 없거나 불완전하면 기능 우선 MVP로 구현하되, DESIGN_AUDIT.md에 부족한 항목을 기록하라.
```


## 7. STACK_DECISION 작성 원칙 — 2026-07-09

개인 PC 로컬 단일 사용자 앱에서는 배포·유지보수 단순성이 중요하다. getdesign.md가 React 컴포넌트 등 특정 스택의 실제 코드를 제공하지 않는 한, 코딩 에이전트가 임의로 React+Vite 같은 무거운 SPA를 선택하지 않도록 한다.

판단 기준:

```text
1. getdesign.md 자산 형태: 컴포넌트 코드 / CSS 토큰 / 스크린샷 / 디자인 설명
2. 백엔드 템플릿과의 결합 난이도
3. Windows PC 로컬 실행 시 설치·빌드 복잡도
4. 접근성 구현 난이도
5. 검색 중심 화면에서 필요한 상호작용 수준
```

기본 권장: 서버 렌더링 + 경량 JS(htmx 또는 vanilla). 단, getdesign.md가 특정 SPA 컴포넌트 코드를 제공하거나 split view/상태 관리 요구가 크게 늘어나는 경우에는 STACK_DECISION.md에 이유를 적고 SPA를 선택할 수 있다.
