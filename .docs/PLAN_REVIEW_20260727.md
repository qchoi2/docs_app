# 계획 리뷰 — V4 실행 궤적 점검 (2026-07-27, Claude)

_실제 `cs_index/catalog.sqlite`를 읽기 전용으로 조회해 계획(V4_PLAN·docs_progress_v2)과
대조한 결과. progress 로그의 마지막 스냅샷(47k item/669문서)보다 실제는 더 진행돼 있었다._

## 실측 현황 (2026-07-27)

| 항목 | 실측값 | 비고 |
|---|---|---|
| V4 원자 item | **100,207개** (전량 approved) | 평가 문서 **950개** |
| taxonomy | **v19 / 414 노드** | 계획 문서엔 v14로 기록됨 |
| 대상유형 진행률 | **782 / 1,623 = 48%** | 아래 유형별 참조 |
| pending 후보 | **29,807개** | 서로 다른 이름 **13,199종** |
| 후보 사람처리 | approved 31 + rejected 16 = **47개** | merged 1,540 |
| 부재 질의 가능 문서 | **742개** | 차단(annex partial/미평가) **567개** |

### 대상유형 진행률 — 극심한 쏠림
- SPA **660/672 (98%)**
- SSA **103/365 (28%)**
- SHA **7/375 (1.9%)**
- ATA/BTA **12/211 (5.7%)**
- 계획에 없던 유형으로 새어나감: CB 118, BW 33, EB 13 등 (scope drift)

## 진단 (쉬운 말)

### ① "자기 시험지를 자기가 채점" — 게이트가 무력화됨
V4_PLAN §9는 **Gate B**(v3+정독 vs V4 하이브리드를 독립 골든 질의로 비교)를 전량 확장의
**조건**으로 뒀고, "우위가 없으면 확장 중단·축소판 전환"이라고 명시했다. 그러나 현재 Gate B는
**V4가 만든 승인 item으로 질의를 만들어 V4로 찾는** 자기참조 구조라 recall이 **구조적으로 항상
1.0000**이다. 즉 "이 비싼 작업을 계속할 가치가 있나"라는 원래 질문에 답하지 못한 채 확장(950문서,
10만 item)이 진행됐다. **Gate A도 미통과**라고 progress에 반복 명시돼 있다.

### ② 후보 백로그 3만 개 — 대표 기능을 꺼뜨림, 그리고 묶어도 안 줄어듦
pending 후보가 **29,807개**로 폭증했고 사람이 처리한 건 47개(0.16%)다. 미해소 후보가 남은
family는 coverage가 `partial`로 묶여 **`confirmed_absent`(없는 계약 찾기)를 반환할 수 없다** —
평가문서의 43%(567/1,309)에서 대표 기능이 꺼져 있다.

당초 "후보를 문구별로 묶어 UI-5 버튼으로 일괄 처리"를 권했으나, **실측 결과 불가능**하다:
서로 다른 이름이 **13,199종**, 상위 25개 문구가 전체의 1%에 불과하다. 게다가 6,035개가
**DEF(정의조항)** — "정의 제18항"처럼 **문서마다 다른 문서-특정 항목**이라 애초에 재사용
가능한 통제 taxonomy 후보가 아니다. 즉 문제는 처리 속도가 아니라 **후보 생성 기준이 너무
느슨**하다는 설계 결함이다.

중요한 완화 요소: V4_PLAN 원칙 5·§4에 따라 미분류 후보 item도 `v4_item_fts`에 들어가
**텍스트로는 검색된다**. 따라서 3만 후보가 "검색 불가"는 아니다. 유일한 실질 피해는
**coverage=partial → 부재 질의 차단**이다.

> **⚠️ 정정 (2026-07-29, 실측)**: 위 "완화 요소" 문단은 **틀렸다.** `v4_item_fts`는
> `v4_clause_item`의 트리거로만 채워지는데, `finalize_result`는 미해소 후보를 item 생성
> **없이** 후보 테이블에만 append한다. 실측 결과 **pending 29,807건 중 27,979건(93.9%)이
> 대응 clause item이 아예 없어** 그 텍스트는 FTS로 검색되지 않는다. 즉 피해는 부재 질의
> 차단에 그치지 않고 **원칙 5(미분류도 텍스트로는 찾힌다)가 실제로는 지켜지지 않고 있었다.**
> T-D ②의 catch-all 흡수가 이 공백을 함께 닫는다. 아래 "항목 2 심화"의 결함 목록에
> 이 항목을 추가로 읽어라.

### ③ "혼자 쓸 도구"인데 규모가 커졌고, 절반에서 멈춤 판단 근거가 없음
목표는 개인용·절약형인데 V4는 10만 item으로 커졌고 대상의 48%에서 멈춰 있다. Gate B가 원래
"여기서 멈출까"를 묻는 자리였는데 우회되면서 나머지 52%를 같은 강도로 갈 근거가 없다.

### ④ 결정의 자(尺)가 낡음
"모든 결정은 골든 질의 재현율로 — 감 금지"가 헌법인데 골든 세트는 초기 ~33개에서 거의 안 컸고
V4 게이트 36개는 자동생성이다. 자가 부실해서 ①의 순환 문제가 생겼다.

## 권고 시퀀스 (순서대로)

_아래 6개 권고의 **현재 이행 상태는 [NOW.md](../NOW.md)에서 추적**한다(단일 원천). 이 문서는 권고의 근거와 당시 실측을 유지한다.
2026-07-29 기준 요약: 1 이행 / 2 도구 구현 완료·검증 진행 / 3 질의시점 decouple만 완료 / 4 확장 재개까지 보류 / 5 진행 중 / 6 미이행._

1. **확장 일시 정지** — 다음 300건 배치 전에 아래 2·3을 먼저 한다.
2. **독립 Gate B — 풀링(pooling) + 사람 검증** (자 교체·진짜 Gate B):
   소유자는 코퍼스 전체 정답을 손으로 채울 수 없다(특히 부재형은 전수 정독 필요). 대신
   **표준 IR 방법인 풀링**을 쓴다 — 같은 질의를 (a) v3+정독, (b) V4 하이브리드 두 방식으로
   돌려 결과를 합치고, 소유자는 **그 합쳐진 pool의 각 항목이 문항에 맞는지만** 판정한다
   (전수 아님, 제시된 것만). 이로부터 **precision**(각 방식 결과의 정확도)과
   **상대 재현율**(사람이 맞다고 본 pool 중 각 방식이 잡은 비율)을 자동 산출해
   "어느 방식이 더 잘 찾나"를 정독 전수 없이 비교한다. 부재형 pool 항목은 그 계약의 해당
   조항 위치만 열어 "정말 없나"를 확인하고, 별지 미평가/판독불가는 unknown으로 분리한다.
   질의 초안 seed는 `data/golden_queries_v4_independent.seed.yaml`(존재 8·부재 8·비교 6,
   `method: pooled_verification`). eval_v4_gate.py에 **pooled 모드**를 추가해 연결한다.
   결과에서 V4 우위 확인 시 확장, 아니면 계획대로 축소판 전환.
3. **후보 생성 기준 교정** (백로그 근본 대응) — 소유자 결정 필요:
   - (a) DEF 정의항 등 문서-특정 항목을 taxonomy **후보에서 제외**(FTS item으로만 유지)하는
     규칙을 추가하고, 기존 DEF 계열 pending을 일괄 재분류.
   - (b) coverage/부재 판정에서 **"문서-특정 후보는 partial 사유에서 제외"** 규칙을 넣어,
     문서-특정 항목이 family 전체의 부재 질의를 차단하지 않게 한다.
   이 둘은 부재 질의를 되살리는 핵심이며, §5 거버넌스(버튼) 밖의 규칙 변경이라 소유자 승인이 필요.
4. **유형 균형 재조정** — SPA 98%인데 SHA 2%·ATA/BTA 6%다. 확장을 재개한다면 계획의
   SPA→SSA→SHA→ATA/BTA 순서를 실제로 지켜 SHA/ATA를 끌어올린다. 계획 외 유형(CB/BW/EB)
   추가 편입은 중단하거나 명시적으로 계획에 반영한다.
5. **번다운 지표 상설화** — "대상유형 782/1,623, family별 complete/partial/미평가, 부재가능
   742/차단 567"을 progress 요약 또는 웹 대시보드에 상시 노출해 "얼마나 남았나·왜 막혔나"를
   역산 없이 본다.
6. **T4(벡터) 로드맵 강등** — "구현 확정"을 "V4 안정 후 재평가 대기"로 명시. 설계 원칙이 이미
   이 코퍼스에서 임베딩 효용을 낮게 봤다.

## 항목 2 심화 — 후보 생성 결함 (소유자 결정 반영 후 실측, 2026-07-27)

소유자 결정: "정의조항 용어는 후보로 유지하되, 계약 특유의 일회성 정의는 후보에서 제외하고
**계약상 일반적으로 쓰이는 용어만** 후보로." 이를 적용하려고 실측한 결과 두 가지 결함 확인:

1. **`document_count`가 전부 1이다.** pending 29,807개 **전량이 document_count=1** — 계획 §2가
   약속한 "발견 문서 수 자동 갱신"이 구현/작동하지 않는다. 따라서 "일반 용어(다수 문서 반복)
   vs 일회성(단일 문서)"을 이 필드로 판별할 수 없다.
2. **후보가 용어명이 아니라 위치(순번)로 명명된다.** 아래 예시 문자열은 부정확했다 —
   **실제 패턴은 `검토후보: 본문 ¶18 명제` / `검토후보: 별지 1 ¶249 별지 명제`이고,
   pending 29,807건의 100%가 이 위치 기반 형태다(DEF 한정 결함이 아니라 backlog 전체다).**
   (2026-07-29 실측 정정. 아래 원문은 이력으로 보존한다.) 예: `정의조항: 정의 제18항`.
   같은 "제18항"이 30개 문서에서 잡히므로 이름으로는 반복처럼 보이지만 의미는 문서마다
   다르다. 즉 이름 기반 재사용성 판별이 불가능하고, 정의 목차의 모든 항을 기계적으로
   후보화해 6,035개 DEF 노이즈가 쌓였다.

**함의**: 결정 #2는 빠른 패치로 못 고친다. 근본 교정이 필요하다 —
- (교정 A) 추출/후보 생성 시 **DEF 후보를 정의된 용어명으로 명명**하고, 계약 일반 용어만
  후보화(문서 특유 일회성 정의는 FTS item으로만 유지, 후보 생성 안 함).
- (교정 B) `document_count`를 **정규화 용어 기준 실제 반복 문서 수로 갱신**하고, 부재 차단은
  일정 반복 이상(generic)만 적용. 위치명 DEF는 반복으로 오판되지 않도록 용어 정규화가 선행.

두 교정 모두 재추출/후보 재생성을 수반하고, **현재 병행 세션이 후보를 활발히 만지는 중**이라
운영 DB에 즉흥 규칙을 넣으면 충돌·오답(잘못된 confirmed_absent) 위험이 있다. 병행 세션 중단
후 단독으로 (교정 A→B) 순서로 진행하는 것을 권한다. 그 전까지 부재 질의는 계속 `needs_review`
보존이 안전하다.

## Gate B 도구 상태 (2026-07-27 구현 완료)
- `eval_v4_gate.py --pooled --seed data/golden_queries_v4_independent.seed.yaml
  [--pool-depth N] [--worklist out.json]` 로 pooled 독립 Gate B를 실행한다.
- 22개 질의 전부 taxonomy 바인딩됨. pool-depth 25에서 소유자 검증 대상은 **총 834건**
  (사람이 질의별로 처리 가능한 규모). pool_verified를 채우면 precision·상대재현율이 산출된다.
- 초기 신호: **V4E08(R&W보험, COV.RWI)은 V4 구조화 검색 0건** vs legacy 790건 —
  RWI 원자 item 추출/승인 공백 가능성. 독립 Gate B가 드러낸 첫 실질 gap.

## 소유자 결정 (2026-07-27 확정)
- **① Gate B 먼저.** 병행 V4 확장 세션은 중단. → pooled Gate B 도구 구현 완료(위).
  소유자가 `pool_verified`를 채운 뒤 확장 계속/축소판 전환을 판정한다(V4_PLAN §11).
- **② 후보 기준**: 정의조항 용어는 후보로 유지하되 계약 특유 일회성 정의는 제외, 일반 용어만.
  → 실측 결과 document_count 미갱신·DEF 위치명 결함 발견(위 "항목 2 심화"). 병행 세션 중단
  후 교정 A(용어명 명명)→B(반복수 갱신) 순으로 단독 진행. **미완 — 다음 작업.**
- **③ CB/BW/EB 편입 확정.** V4_PLAN §6에 이미 반영됨(전 항목 추출 대상).
